import UIUpdates from '../src/ui-updates';

describe('UIUpdates', () => {
    it('should create', () => {
        const uiUpdates = new UIUpdates();
        expect(uiUpdates).toBeTruthy();
    });
});
